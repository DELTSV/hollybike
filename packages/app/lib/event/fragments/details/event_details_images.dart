import 'package:flutter/material.dart';

class EventDetailsImages extends StatelessWidget {
  const EventDetailsImages({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
