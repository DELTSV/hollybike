import 'package:flutter/cupertino.dart';

class JourneyLibrary extends StatelessWidget {
  const JourneyLibrary({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      height: 200,
      child: Placeholder(),
    );
  }
}
