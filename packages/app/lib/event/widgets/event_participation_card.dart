import 'package:flutter/material.dart';

class EventParticipationCard extends StatelessWidget {
  const EventParticipationCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
